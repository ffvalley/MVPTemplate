package ${packageName}.contract;

import com.tustrip.common.app.IPresenter;
import com.tustrip.common.app.IView;

public interface ${templateName}Contract {
    interface View extends IView {

    }

    interface Presenter extends IPresenter {

    }
}
